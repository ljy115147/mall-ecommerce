# ADR-0004：独立库存中心

- 状态：Accepted
- 日期：2026-07-16

## 背景

库存散落在商品表会导致秒杀、对账、拼团回滚难以追溯。

## 决策

独立 `stock`/`stock_log` 领域；商品目录只描述「卖什么」，库存中心描述「能卖多少」。

## 后果

多一次调用/模块依赖；换来流水、对账与秒杀对齐能力。

## 验证方式

对账 Job；超卖断言；biz_no 幂等。
