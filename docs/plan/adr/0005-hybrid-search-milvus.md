# ADR-0005：ES + Milvus 混合检索

- 状态：Accepted
- 日期：2026-07-16

## 背景

纯关键词无法很好服务语义 Query；纯向量缺少过滤与可运维性。

## 决策

ES 负责关键词与过滤；Milvus 负责语义召回；在线融合；向量路径可降级。

## 后果

双管道运维；需 embedding 服务；延迟预算要严格。

## 验证方式

语义 Query 评估集；杀 Milvus 搜索仍可用。
